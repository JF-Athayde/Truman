# Projeto Truman — instruções rápidas

- Instale dependências:

```bash
pip install -r requirements.txt
```

- Execute a partir da raiz do projeto:

```bash
python main.py
```

Observação: os pacotes `truman` e `anubia` foram convertidos em pacotes Python com `__init__.py`.